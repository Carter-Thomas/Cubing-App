import { A as Alg, a as AlgNode } from './Alg-3ed1d42c.js';

interface ParserIndexed {
    startCharIndex: number;
    endCharIndex: number;
}
type Parsed<T extends Alg | AlgNode> = T & ParserIndexed;

export { Parsed as P };
